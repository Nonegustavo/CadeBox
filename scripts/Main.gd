extends Node

var code = ["AA", "BB", "CC"]
var actualTask = "Tarefa"
var input_list = [] # lista de lista
var output_list = [] # lista de lista
var mode = "sandbox"
var normal_speed = 0.25
var fast_speed = 0.001
var actualCode = 0

# Called when the node enters the scene tree for the first time.
func _ready():
	pass # Replace with function body.


# Called every frame. 'delta' is the elapsed time since the previous frame.
#func _process(delta):
#	pass
